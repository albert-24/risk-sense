server {
    listen 80;
    server_name docker.gates-gis-chat-app;

    root /usr/share/nginx/html;
    index index.html;

    access_log /var/log/nginx/gates-gis-access.log;
    error_log /var/log/nginx/gates-gis-error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
